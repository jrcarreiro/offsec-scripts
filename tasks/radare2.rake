# tasks/radare2.rake

namespace :radare2 do
  namespace :r2lldb do
    desc "Install r2lldb plugin for radare2"
    task :install do
      puts "🔧 Installing r2lldb plugin..."
      # Installation logic goes here
    end
  end
end
